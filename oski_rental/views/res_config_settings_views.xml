<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form_rental" model="ir.ui.view">
        <field name="name">res.config.settings.form.rental</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="Location" string="Location" name="oski_rental">
                    <block title="Facturation">
                        <setting string="Article de facturation"
                                 help="Article service porté par défaut sur les factures de location">
                            <field name="rental_default_product_id"/>
                        </setting>
                        <setting string="Facturer les retards par défaut">
                            <field name="rental_late_invoicing"/>
                        </setting>
                    </block>
                    <block title="Tarification">
                        <setting string="Granularité minimale"
                                 help="En mode Jour, le palier horaire est ignoré">
                            <field name="rental_min_granularity"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

    <record id="action_rental_config_settings" model="ir.actions.act_window">
        <field name="name">Paramètres</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="context">{'module': 'oski_rental'}</field>
    </record>
</odoo>
