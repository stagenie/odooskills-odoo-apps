<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="catalog_page" name="Catalogue des modules">
        <t t-call="website.layout">
            <div class="oski-store">
                <div class="container my-5">
                    <div class="oski-hero">
                        <h1 class="mb-2">OdooSkills Apps</h1>
                        <p>Modules Odoo <t t-out="version"/> prêts à l'emploi — gratuits et premium, signés OdooSkills.</p>
                        <div class="oski-version-pills" role="navigation" aria-label="Version Odoo">
                            <t t-foreach="pill_versions" t-as="pv">
                                <t t-set="q" t-value="'/apps?v=' + pv"/>
                                <t t-if="active_category" t-set="q" t-value="q + '&amp;category=' + str(active_category)"/>
                                <t t-if="search" t-set="q" t-value="q + '&amp;search=' + search"/>
                                <a t-att-href="q" t-attf-class="oski-pill #{'is-active' if pv == version else ''}">
                                    <t t-out="pv"/>
                                </a>
                            </t>
                        </div>
                    </div>
                    <form method="get" action="/apps" class="oski-search mb-4">
                        <div class="input-group">
                            <input type="text" name="search" class="form-control"
                                   placeholder="Rechercher un module…" t-att-value="search"/>
                            <button type="submit" class="btn">Rechercher</button>
                        </div>
                    </form>
                    <div class="row">
                        <div class="col-md-3">
                            <div class="list-group">
                                <a href="/apps" t-attf-class="list-group-item list-group-item-action #{'active' if not active_category else ''}">
                                    Toutes les catégories
                                </a>
                                <t t-foreach="categories" t-as="cat">
                                    <a t-attf-href="/apps?category=#{cat.id}"
                                       t-attf-class="list-group-item list-group-item-action #{'active' if active_category == cat.id else ''}">
                                        <t t-out="cat.name"/>
                                    </a>
                                </t>
                            </div>
                        </div>
                        <div class="col-md-9">
                            <div class="row">
                                <t t-foreach="modules" t-as="module">
                                    <div class="col-md-4 mb-4">
                                        <t t-set="avail" t-value="module.available_versions()"/>
                                        <div t-attf-class="card oski-card h-100 #{'is-incompatible' if version not in avail else ''}">
                                            <div class="card-body">
                                                <h5 class="card-title">
                                                    <a t-att-href="module.website_url"><t t-out="module.name"/></a>
                                                </h5>
                                                <p class="card-text text-muted"><t t-out="module.summary"/></p>
                                                <span t-if="module.is_free" class="badge text-bg-success">Gratuit</span>
                                                <span t-else="" class="badge text-bg-warning">Premium</span>
                                                <div class="oski-pastilles mt-2" aria-label="Compatibilité">
                                                    <t t-foreach="pill_versions" t-as="pv">
                                                        <span t-attf-class="oski-dot #{'on' if pv in avail else 'off'}" t-out="pv"/>
                                                    </t>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </t>
                                <t t-if="not modules">
                                    <p class="text-muted">Aucun module disponible pour l'instant.</p>
                                </t>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </t>
    </template>
</odoo>
